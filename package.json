{
  "name": "@tailwind-rc/date-picker",
  "version": "0.1.1",
  "main": "dist/index.js",
  "module": "dist/module.js",
  "types": "dist/index.d.ts",
  "repository": "git@github.com:Apozhidaev/tailwindcss-date-picker.git",
  "author": "alexanderpo <alexander.pozhidaev@gmail.com>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "route",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf dist/common",
    "build:ts": "tsc && yarn run clean",
    "build:cjs": "esbuild src/index.tsx --format=cjs --bundle --outfile=dist/index.js --external:@heroicons/* --external:react* --external:classnames",
    "build:esm": "esbuild src/index.tsx --format=esm --bundle --outfile=dist/module.js --external:@heroicons/* --external:react* --external:classnames",
    "build:route:cjs": "esbuild src/route/index.tsx --format=cjs --bundle --outfile=route/index.js --external:@heroicons/* --external:react* --external:classnames",
    "build:route:esm": "esbuild src/route/index.tsx --format=esm --bundle --outfile=route/module.js --external:@heroicons/* --external:react* --external:classnames",
    "build": "yarn run build:cjs && yarn run build:esm && yarn run build:route:cjs && yarn run build:route:esm && yarn run build:ts"
  },
  "peerDependencies": {
    "react": ">=16.8.0"
  },
  "dependencies": {
    "@heroicons/react": "^2.0.13",
    "classnames": "^2.3.2",
    "react-ease-picker": "^0.0.3"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "esbuild": "^0.16.9",
    "react": "^18.2.0",
    "react-router-dom": "^6.6.1",
    "typescript": "^4.9.4"
  }
}
